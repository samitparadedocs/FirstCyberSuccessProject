<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cybersuccess.demo.firstcybersuccessproject">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DemoListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivitySecond" />
        <activity android:name=".FirstActivity" />
        <activity android:name=".layoutcomponets.LayoutComponetsActivity" />
        <activity android:name=".layoutdata.LinearLayoutDemo" />
        <activity android:name=".layoutdata.RelativeLayoutDemoAct" />
        <activity android:name=".layoutcomponets.radiobutton.RadioButtonDemoActivity" />
        <activity android:name=".MenusDemoActivity" />
        <activity android:name=".picker.PickerDemoActivity" />
        <activity android:name=".service.ServiceDemoActivity" />

        <service
            android:name=".service.MyService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".sqlite.SqliteDemoActivity"
            android:label="@string/title_activity_sqlite_demo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".sharedpreference.SharedPrefferenceDemoActivity" />
        <activity
            android:name=".sqlite.TempFloatingButtonActivty"
            android:label="@string/title_activity_temp_floating_button_activty"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>